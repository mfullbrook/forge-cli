// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package teams

import (
	"fmt"
	"github.com/mfullbrook/forge-cli/internal/client"
	"github.com/mfullbrook/forge-cli/internal/flagutil"
	"github.com/mfullbrook/forge-cli/internal/interactive"
	"github.com/mfullbrook/forge-cli/internal/output"
	"github.com/mfullbrook/forge-cli/internal/sdk"
	"github.com/mfullbrook/forge-cli/internal/sdk/models/operations"
	"github.com/mfullbrook/forge-cli/internal/usage"
	"github.com/spf13/cobra"
)

var organizationsTeamsStoreCmdMeta = []flagutil.FlagMeta{
	{FlagName: "organization", FieldPath: "Organization", Kind: flagutil.FlagKindString, Required: true, Description: "The organization slug [required]"},
	{FlagName: "name", Shorthand: "n", FieldPath: "Body.Name", Kind: flagutil.FlagKindString, Required: true, Description: "[required]"},
	{FlagName: "users", Shorthand: "u", FieldPath: "Body.Users", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"users,omitempty"`, Description: "list of values"},
	{FlagName: "invites", Shorthand: "i", FieldPath: "Body.Invites", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"invites,omitempty"`, Description: "list of values"},
}

// initOrganizationsTeamsStoreCmd initializes the organizations-teams-store command.
func initOrganizationsTeamsStoreCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "organizations-teams-store",
		Short:   "Create team",
		Long:    "Create a new team for the organization.\n\nProcessing mode: <small><code>sync</code></small>",
		Example: "  forge teams organizations-teams-store --organization <value> --name <value>",
		RunE:    runOrganizationsTeamsStoreCmd,
		Aliases: []string{"otst"},
	}
	flagutil.RegisterFlags(cmd, organizationsTeamsStoreCmdMeta)
	if err := flagutil.ValidateMeta[operations.OrganizationsTeamsStoreRequest](organizationsTeamsStoreCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for organizations-teams-store: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runOrganizationsTeamsStoreCmd executes the organizations-teams-store command.
func runOrganizationsTeamsStoreCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, organizationsTeamsStoreCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, organizationsTeamsStoreCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.OrganizationsTeamsStoreRequest](cmd, organizationsTeamsStoreCmdMeta, "Body", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Teams.OrganizationsTeamsStore(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
