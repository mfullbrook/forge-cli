// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package teams

import (
	"fmt"
	"github.com/mfullbrook/forge-cli/internal/client"
	"github.com/mfullbrook/forge-cli/internal/flagutil"
	"github.com/mfullbrook/forge-cli/internal/output"
	"github.com/mfullbrook/forge-cli/internal/sdk"
	"github.com/mfullbrook/forge-cli/internal/sdk/models/operations"
	"github.com/mfullbrook/forge-cli/internal/usage"
	"github.com/spf13/cobra"
)

var organizationsTeamsUpdateCmdMeta = []flagutil.FlagMeta{
	{FlagName: "organization", FieldPath: "Organization", Kind: flagutil.FlagKindString, Required: true, Description: "The organization slug [required]"},
	{FlagName: "team", Shorthand: "t", FieldPath: "Team", Kind: flagutil.FlagKindInt64, Required: true, Description: "The team ID [required]"},
	{FlagName: "name", Shorthand: "n", FieldPath: "Body.Name", Kind: flagutil.FlagKindString, Required: true, Description: "[required]"},
	{FlagName: "users", Shorthand: "u", FieldPath: "Body.Users", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"users,omitempty"`, Description: "list of values"},
}

// initOrganizationsTeamsUpdateCmd initializes the organizations-teams-update command.
func initOrganizationsTeamsUpdateCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "organizations-teams-update",
		Short:   "Update team",
		Long:    "Update a team for the organization.\n\nProcessing mode: <small><code>sync</code></small>",
		Example: "  forge teams organizations-teams-update --organization <value> --team 697578 --name <value>",
		Args:    cobra.NoArgs,
		RunE:    runOrganizationsTeamsUpdateCmd,
		Aliases: []string{"otu"},
		Annotations: map[string]string{
			"speakeasy_operation": "organizations.teams.update",
		},
	}
	flagutil.RegisterFlags(cmd, organizationsTeamsUpdateCmdMeta)
	if err := flagutil.ValidateMeta[operations.OrganizationsTeamsUpdateRequest](organizationsTeamsUpdateCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for organizations-teams-update: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin; @path reads a file, @- reads stdin to EOF. Use --schema to print the exact JSON Schema.")
	_ = flagutil.AnnotatePromptFlag(cmd, "body", flagutil.PromptFlagSpec{Kind: "json", BodyFlag: true})
	cmd.Annotations[flagutil.AnnotationWholeBodyFlag] = "body"
	if err := flagutil.AnnotateBodyFields(cmd, organizationsTeamsUpdateCmdMeta, "Body", "body"); err != nil {
		return fmt.Errorf("annotate body fields for organizations-teams-update: %w", err)
	}
	cmd.Flags().Bool("schema", false, "Print the exact JSON Schema of the request body and exit")
	_ = flagutil.AnnotatePromptFlag(cmd, "schema", flagutil.PromptFlagSpec{Kind: "bool", DocSurface: true})
	parent.AddCommand(cmd)
	return nil
}

// runOrganizationsTeamsUpdateCmd executes the organizations-teams-update command.
func runOrganizationsTeamsUpdateCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if requested, _ := cmd.Flags().GetBool("schema"); requested {
		return usage.EmitBodySchema(cmd.OutOrStdout(), "organizations.teams.update")
	}
	req, err := flagutil.BuildRequest[operations.OrganizationsTeamsUpdateRequest](cmd, organizationsTeamsUpdateCmdMeta, "Body", "body")
	if err != nil {
		return flagutil.WithCLIValidation(err)
	}
	s, err := client.NewClient(cmd, "Oauth2")
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Teams.OrganizationsTeamsUpdate(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
