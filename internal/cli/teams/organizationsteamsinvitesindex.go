// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package teams

import (
	"fmt"
	"github.com/mfullbrook/forge-cli/internal/client"
	"github.com/mfullbrook/forge-cli/internal/flagutil"
	"github.com/mfullbrook/forge-cli/internal/interactive"
	"github.com/mfullbrook/forge-cli/internal/output"
	"github.com/mfullbrook/forge-cli/internal/sdk"
	"github.com/mfullbrook/forge-cli/internal/sdk/models/operations"
	"github.com/mfullbrook/forge-cli/internal/usage"
	"github.com/spf13/cobra"
)

var organizationsTeamsInvitesIndexCmdMeta = []flagutil.FlagMeta{
	{FlagName: "organization", FieldPath: "Organization", Kind: flagutil.FlagKindString, Required: true, Description: "The organization slug [required]"},
	{FlagName: "team", Shorthand: "t", FieldPath: "Team", Kind: flagutil.FlagKindInt64, Required: true, Description: "The team ID [required]"},
	{FlagName: "include", Shorthand: "i", FieldPath: "Include", Kind: flagutil.FlagKindString, Optional: true, Description: "Available includes are `role`, `roleCount`, `roleExists`, `team`, `teamCount`, `teamExists`, `organization`, `organizationCount`, `organizationExists`. You can include multiple options by separating them with a comma."},
	{FlagName: "page-size", FieldPath: "PageSize", Kind: flagutil.FlagKindInt64, Optional: true, HasDefault: true, DefaultInt: 30, Description: "The number of results that will be returned per page."},
	{FlagName: "page-cursor", FieldPath: "PageCursor", Kind: flagutil.FlagKindString, Optional: true, Description: "The cursor to start the pagination from."},
}

// initOrganizationsTeamsInvitesIndexCmd initializes the organizations-teams-invites-index command.
func initOrganizationsTeamsInvitesIndexCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "organizations-teams-invites-index",
		Short:   "List team invitations",
		Long:    "Show all pending invitations for the team.\n\nProcessing mode: <small><code>sync</code></small>",
		Example: "  forge teams organizations-teams-invites-index --organization <value> --team 395470",
		RunE:    runOrganizationsTeamsInvitesIndexCmd,
		Aliases: []string{"otii"},
	}
	flagutil.RegisterFlags(cmd, organizationsTeamsInvitesIndexCmdMeta)
	if err := flagutil.ValidateMeta[operations.OrganizationsTeamsInvitesIndexRequest](organizationsTeamsInvitesIndexCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for organizations-teams-invites-index: %w", err)
	}
	parent.AddCommand(cmd)
	return nil
}

// runOrganizationsTeamsInvitesIndexCmd executes the organizations-teams-invites-index command.
func runOrganizationsTeamsInvitesIndexCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, organizationsTeamsInvitesIndexCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, organizationsTeamsInvitesIndexCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.OrganizationsTeamsInvitesIndexRequest](cmd, organizationsTeamsInvitesIndexCmdMeta, "", "")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Teams.OrganizationsTeamsInvitesIndex(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
