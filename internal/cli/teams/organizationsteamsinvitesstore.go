// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package teams

import (
	"fmt"
	"github.com/mfullbrook/forge-cli/internal/client"
	"github.com/mfullbrook/forge-cli/internal/flagutil"
	"github.com/mfullbrook/forge-cli/internal/interactive"
	"github.com/mfullbrook/forge-cli/internal/output"
	"github.com/mfullbrook/forge-cli/internal/sdk"
	"github.com/mfullbrook/forge-cli/internal/sdk/models/operations"
	"github.com/mfullbrook/forge-cli/internal/usage"
	"github.com/spf13/cobra"
)

var organizationsTeamsInvitesStoreCmdMeta = []flagutil.FlagMeta{
	{FlagName: "organization", FieldPath: "Organization", Kind: flagutil.FlagKindString, Required: true, Description: "The organization slug [required]"},
	{FlagName: "team", Shorthand: "t", FieldPath: "Team", Kind: flagutil.FlagKindInt64, Required: true, Description: "The team ID [required]"},
	{FlagName: "role-id", Shorthand: "r", FieldPath: "Body.RoleID", Kind: flagutil.FlagKindInt64, Required: true, Description: "[required]"},
	{FlagName: "email", Shorthand: "e", FieldPath: "Body.Email", Kind: flagutil.FlagKindString, Required: true, Description: "[required]"},
}

// initOrganizationsTeamsInvitesStoreCmd initializes the organizations-teams-invites-store command.
func initOrganizationsTeamsInvitesStoreCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "organizations-teams-invites-store",
		Short:   "Create team invite",
		Long:    "Invite a new member to the team.\n\nProcessing mode: <small><code>async</code></small>",
		Example: "  forge teams organizations-teams-invites-store --organization <value> --team 50826 --role-id 234245 --email Brandy.Gorczany29@hotmail.com",
		RunE:    runOrganizationsTeamsInvitesStoreCmd,
		Aliases: []string{"otist"},
	}
	flagutil.RegisterFlags(cmd, organizationsTeamsInvitesStoreCmdMeta)
	if err := flagutil.ValidateMeta[operations.OrganizationsTeamsInvitesStoreRequest](organizationsTeamsInvitesStoreCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for organizations-teams-invites-store: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runOrganizationsTeamsInvitesStoreCmd executes the organizations-teams-invites-store command.
func runOrganizationsTeamsInvitesStoreCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, organizationsTeamsInvitesStoreCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, organizationsTeamsInvitesStoreCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.OrganizationsTeamsInvitesStoreRequest](cmd, organizationsTeamsInvitesStoreCmdMeta, "Body", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Teams.OrganizationsTeamsInvitesStore(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
