// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package cli

import (
	"fmt"
	"github.com/mfullbrook/forge-cli/internal/config"
	"github.com/mfullbrook/forge-cli/internal/usage"
	"github.com/spf13/cobra"
)

// initWhoamiCmd initializes the whoami command.
func initWhoamiCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:   "whoami",
		Short: "Display current authentication configuration",
		Long: `Display the currently configured settings and their sources.

Sources are shown as:
  [flag]    - Set via command line flag
  [env]     - Set via environment variable (FORGE_*)
  [keyring] - Set via OS keychain (stored by configure command)
  [config]  - Set via config file (~/.config/forge/config.yaml)
  [unset]   - Not configured

Credential values are masked for security.`,
		RunE: runWhoamiCmd,
	}
	parent.AddCommand(cmd)
	return nil
}

// runWhoamiCmd executes the whoami command.
func runWhoamiCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	out := cmd.OutOrStdout()
	fmt.Fprintln(out, "Configuration")
	fmt.Fprintln(out, "=============")
	fmt.Fprintln(out)
	fmt.Fprintf(out, "Config file: %s\n", config.GetConfigPath())
	fmt.Fprintf(out, "Environment prefix: FORGE_\n")
	fmt.Fprintln(out)
	fmt.Fprintln(out, "Credentials:")

	// HTTP Bearer
	{
		value, source := config.ResolveSecurityCredential(cmd, "http")
		fmt.Fprintf(out, "  --%-25s [%-7s] %s\n", "http", source, maskSecret(value))
	}

	// OAuth2 Authorization
	{
		value, source := config.ResolveSecurityCredential(cmd, "oauth2")
		fmt.Fprintf(out, "  --%-25s [%-7s] %s\n", "oauth2", source, maskSecret(value))
	}

	return nil
}
