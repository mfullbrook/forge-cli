// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package recipes

import (
	"fmt"
	"github.com/mfullbrook/forge-cli/internal/client"
	"github.com/mfullbrook/forge-cli/internal/flagutil"
	"github.com/mfullbrook/forge-cli/internal/interactive"
	"github.com/mfullbrook/forge-cli/internal/output"
	"github.com/mfullbrook/forge-cli/internal/sdk"
	"github.com/mfullbrook/forge-cli/internal/sdk/models/operations"
	"github.com/mfullbrook/forge-cli/internal/usage"
	"github.com/spf13/cobra"
)

var organizationsRecipesShowCmdMeta = []flagutil.FlagMeta{
	{FlagName: "organization", FieldPath: "Organization", Kind: flagutil.FlagKindString, Required: true, Description: "The organization slug [required]"},
	{FlagName: "recipe", Shorthand: "r", FieldPath: "Recipe", Kind: flagutil.FlagKindInt64, Required: true, Description: "The recipe ID [required]"},
}

// initOrganizationsRecipesShowCmd initializes the organizations-recipes-show command.
func initOrganizationsRecipesShowCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "organizations-recipes-show",
		Short:   "Get recipe",
		Long:    "Show the recipe for the organization.\n\nProcessing mode: <small><code>sync</code></small>",
		Example: "  forge recipes organizations-recipes-show --organization <value> --recipe 215517",
		RunE:    runOrganizationsRecipesShowCmd,
		Aliases: []string{"orsh"},
	}
	flagutil.RegisterFlags(cmd, organizationsRecipesShowCmdMeta)
	if err := flagutil.ValidateMeta[operations.OrganizationsRecipesShowRequest](organizationsRecipesShowCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for organizations-recipes-show: %w", err)
	}
	parent.AddCommand(cmd)
	return nil
}

// runOrganizationsRecipesShowCmd executes the organizations-recipes-show command.
func runOrganizationsRecipesShowCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, organizationsRecipesShowCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, organizationsRecipesShowCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.OrganizationsRecipesShowRequest](cmd, organizationsRecipesShowCmdMeta, "", "")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Recipes.OrganizationsRecipesShow(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
