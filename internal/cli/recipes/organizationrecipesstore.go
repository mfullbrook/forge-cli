// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package recipes

import (
	"fmt"
	"github.com/mfullbrook/forge-cli/internal/client"
	"github.com/mfullbrook/forge-cli/internal/flagutil"
	"github.com/mfullbrook/forge-cli/internal/interactive"
	"github.com/mfullbrook/forge-cli/internal/output"
	"github.com/mfullbrook/forge-cli/internal/sdk"
	"github.com/mfullbrook/forge-cli/internal/sdk/models/operations"
	"github.com/mfullbrook/forge-cli/internal/usage"
	"github.com/spf13/cobra"
)

var organizationRecipesStoreCmdMeta = []flagutil.FlagMeta{
	{FlagName: "organization", FieldPath: "Organization", Kind: flagutil.FlagKindString, Required: true, Description: "The organization slug [required]"},
	{FlagName: "name", Shorthand: "n", FieldPath: "Body.Name", Kind: flagutil.FlagKindString, Required: true, Description: "[required]"},
	{FlagName: "user", Shorthand: "u", FieldPath: "Body.User", Kind: flagutil.FlagKindEnum, Required: true, EnumValues: []string{"root", "forge"}, Description: "options: root, forge [required]"},
	{FlagName: "script", Shorthand: "s", FieldPath: "Body.Script", Kind: flagutil.FlagKindString, Required: true, Description: "[required]"},
	{FlagName: "team-id", Shorthand: "t", FieldPath: "Body.TeamID", Kind: flagutil.FlagKindInt64, Optional: true, Description: "integer value"},
}

// initOrganizationRecipesStoreCmd initializes the organization-recipes-store command.
func initOrganizationRecipesStoreCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "organization-recipes-store",
		Short:   "Create recipe",
		Long:    "Create a new recipe for the organization.\n\nProcessing mode: <small><code>sync</code></small>",
		Example: "  forge recipes organization-recipes-store --organization <value> --name <value> --user root --script <value>",
		RunE:    runOrganizationRecipesStoreCmd,
		Aliases: []string{"ors"},
	}
	flagutil.RegisterFlags(cmd, organizationRecipesStoreCmdMeta)
	if err := flagutil.ValidateMeta[operations.OrganizationRecipesStoreRequest](organizationRecipesStoreCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for organization-recipes-store: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runOrganizationRecipesStoreCmd executes the organization-recipes-store command.
func runOrganizationRecipesStoreCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, organizationRecipesStoreCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, organizationRecipesStoreCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.OrganizationRecipesStoreRequest](cmd, organizationRecipesStoreCmdMeta, "Body", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Recipes.OrganizationRecipesStore(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
