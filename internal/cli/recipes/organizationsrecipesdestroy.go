// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package recipes

import (
	"fmt"
	"github.com/mfullbrook/forge-cli/internal/client"
	"github.com/mfullbrook/forge-cli/internal/flagutil"
	"github.com/mfullbrook/forge-cli/internal/interactive"
	"github.com/mfullbrook/forge-cli/internal/output"
	"github.com/mfullbrook/forge-cli/internal/sdk"
	"github.com/mfullbrook/forge-cli/internal/sdk/models/operations"
	"github.com/mfullbrook/forge-cli/internal/usage"
	"github.com/spf13/cobra"
)

var organizationsRecipesDestroyCmdMeta = []flagutil.FlagMeta{
	{FlagName: "organization", FieldPath: "Organization", Kind: flagutil.FlagKindString, Required: true, Description: "The organization slug [required]"},
	{FlagName: "recipe", Shorthand: "r", FieldPath: "Recipe", Kind: flagutil.FlagKindInt64, Required: true, Description: "The recipe ID [required]"},
}

// initOrganizationsRecipesDestroyCmd initializes the organizations-recipes-destroy command.
func initOrganizationsRecipesDestroyCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "organizations-recipes-destroy",
		Short:   "Delete recipe",
		Long:    "Delete a recipe from the organization.\n\nProcessing mode: <small><code>sync</code></small>",
		Example: "  forge recipes organizations-recipes-destroy --organization <value> --recipe 455109",
		RunE:    runOrganizationsRecipesDestroyCmd,
		Aliases: []string{"ord"},
	}
	flagutil.RegisterFlags(cmd, organizationsRecipesDestroyCmdMeta)
	if err := flagutil.ValidateMeta[operations.OrganizationsRecipesDestroyRequest](organizationsRecipesDestroyCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for organizations-recipes-destroy: %w", err)
	}
	parent.AddCommand(cmd)
	return nil
}

// runOrganizationsRecipesDestroyCmd executes the organizations-recipes-destroy command.
func runOrganizationsRecipesDestroyCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, organizationsRecipesDestroyCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, organizationsRecipesDestroyCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.OrganizationsRecipesDestroyRequest](cmd, organizationsRecipesDestroyCmdMeta, "", "")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Recipes.OrganizationsRecipesDestroy(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
