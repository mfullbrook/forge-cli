// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package recipes

import (
	"fmt"
	"github.com/mfullbrook/forge-cli/internal/client"
	"github.com/mfullbrook/forge-cli/internal/flagutil"
	"github.com/mfullbrook/forge-cli/internal/interactive"
	"github.com/mfullbrook/forge-cli/internal/output"
	"github.com/mfullbrook/forge-cli/internal/sdk"
	"github.com/mfullbrook/forge-cli/internal/sdk/models/operations"
	"github.com/mfullbrook/forge-cli/internal/usage"
	"github.com/spf13/cobra"
)

var forgeRecipesIndexCmdMeta = []flagutil.FlagMeta{
	{FlagName: "page-size", FieldPath: "PageSize", Kind: flagutil.FlagKindInt64, Optional: true, HasDefault: true, DefaultInt: 30, Description: "The number of results that will be returned per page."},
	{FlagName: "page-cursor", FieldPath: "PageCursor", Kind: flagutil.FlagKindString, Optional: true, Description: "The cursor to start the pagination from."},
}

// initForgeRecipesIndexCmd initializes the forge-recipes-index command.
func initForgeRecipesIndexCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "forge-recipes-index",
		Short:   "List Forge's recipes",
		Long:    "Show all Forge provided recipes.\n\nProcessing mode: <small><code>sync</code></small>",
		Example: "  forge recipes forge-recipes-index",
		RunE:    runForgeRecipesIndexCmd,
		Aliases: []string{"fri"},
	}
	flagutil.RegisterFlags(cmd, forgeRecipesIndexCmdMeta)
	if err := flagutil.ValidateMeta[operations.ForgeRecipesIndexRequest](forgeRecipesIndexCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for forge-recipes-index: %w", err)
	}
	parent.AddCommand(cmd)
	return nil
}

// runForgeRecipesIndexCmd executes the forge-recipes-index command.
func runForgeRecipesIndexCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, forgeRecipesIndexCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, forgeRecipesIndexCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.ForgeRecipesIndexRequest](cmd, forgeRecipesIndexCmdMeta, "", "")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Recipes.ForgeRecipesIndex(cmd.Context(), req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
