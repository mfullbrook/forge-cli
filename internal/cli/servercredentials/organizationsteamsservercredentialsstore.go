// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package servercredentials

import (
	"fmt"
	"github.com/mfullbrook/forge-cli/internal/client"
	"github.com/mfullbrook/forge-cli/internal/flagutil"
	"github.com/mfullbrook/forge-cli/internal/interactive"
	"github.com/mfullbrook/forge-cli/internal/output"
	"github.com/mfullbrook/forge-cli/internal/sdk"
	"github.com/mfullbrook/forge-cli/internal/sdk/models/operations"
	"github.com/mfullbrook/forge-cli/internal/usage"
	"github.com/spf13/cobra"
)

var organizationsTeamsServerCredentialsStoreCmdMeta = []flagutil.FlagMeta{
	{FlagName: "organization", FieldPath: "Organization", Kind: flagutil.FlagKindString, Required: true, Description: "The organization slug [required]"},
	{FlagName: "team", Shorthand: "t", FieldPath: "Team", Kind: flagutil.FlagKindInt64, Required: true, Description: "The team ID [required]"},
	{FlagName: "credential-id", Shorthand: "c", FieldPath: "Body.CredentialID", Kind: flagutil.FlagKindInt64, Required: true, Description: "[required]"},
}

// initOrganizationsTeamsServerCredentialsStoreCmd initializes the organizations-teams-server-credentials-store command.
func initOrganizationsTeamsServerCredentialsStoreCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "organizations-teams-server-credentials-store",
		Short:   "Create a new server credential share",
		Long:    "Share a server credential with a team.\n\nProcessing mode: <small><code>sync</code></small>",
		Example: "  forge server-credentials organizations-teams-server-credentials-store --organization <value> --team 503589 --credential-id 710187",
		RunE:    runOrganizationsTeamsServerCredentialsStoreCmd,
		Aliases: []string{"otscs"},
	}
	flagutil.RegisterFlags(cmd, organizationsTeamsServerCredentialsStoreCmdMeta)
	if err := flagutil.ValidateMeta[operations.OrganizationsTeamsServerCredentialsStoreRequest](organizationsTeamsServerCredentialsStoreCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for organizations-teams-server-credentials-store: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runOrganizationsTeamsServerCredentialsStoreCmd executes the organizations-teams-server-credentials-store command.
func runOrganizationsTeamsServerCredentialsStoreCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, organizationsTeamsServerCredentialsStoreCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, organizationsTeamsServerCredentialsStoreCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.OrganizationsTeamsServerCredentialsStoreRequest](cmd, organizationsTeamsServerCredentialsStoreCmdMeta, "Body", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.ServerCredentials.OrganizationsTeamsServerCredentialsStore(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
