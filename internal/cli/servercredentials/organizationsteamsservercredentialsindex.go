// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package servercredentials

import (
	"fmt"
	"github.com/mfullbrook/forge-cli/internal/client"
	"github.com/mfullbrook/forge-cli/internal/flagutil"
	"github.com/mfullbrook/forge-cli/internal/interactive"
	"github.com/mfullbrook/forge-cli/internal/output"
	"github.com/mfullbrook/forge-cli/internal/sdk"
	"github.com/mfullbrook/forge-cli/internal/sdk/models/operations"
	"github.com/mfullbrook/forge-cli/internal/usage"
	"github.com/spf13/cobra"
)

var organizationsTeamsServerCredentialsIndexCmdMeta = []flagutil.FlagMeta{
	{FlagName: "organization", FieldPath: "Organization", Kind: flagutil.FlagKindString, Required: true, Description: "The organization slug [required]"},
	{FlagName: "team", Shorthand: "t", FieldPath: "Team", Kind: flagutil.FlagKindInt64, Required: true, Description: "The team ID [required]"},
	{FlagName: "page-size", FieldPath: "PageSize", Kind: flagutil.FlagKindInt64, Optional: true, HasDefault: true, DefaultInt: 30, Description: "The number of results that will be returned per page."},
	{FlagName: "page-cursor", FieldPath: "PageCursor", Kind: flagutil.FlagKindString, Optional: true, Description: "The cursor to start the pagination from."},
}

// initOrganizationsTeamsServerCredentialsIndexCmd initializes the organizations-teams-server-credentials-index command.
func initOrganizationsTeamsServerCredentialsIndexCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "organizations-teams-server-credentials-index",
		Short:   "List team server credentials",
		Long:    "Show all server credentials for the team.\n\nProcessing mode: <small><code>sync</code></small>",
		Example: "  forge server-credentials organizations-teams-server-credentials-index --organization <value> --team 460913",
		RunE:    runOrganizationsTeamsServerCredentialsIndexCmd,
		Aliases: []string{"otsci"},
	}
	flagutil.RegisterFlags(cmd, organizationsTeamsServerCredentialsIndexCmdMeta)
	if err := flagutil.ValidateMeta[operations.OrganizationsTeamsServerCredentialsIndexRequest](organizationsTeamsServerCredentialsIndexCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for organizations-teams-server-credentials-index: %w", err)
	}
	parent.AddCommand(cmd)
	return nil
}

// runOrganizationsTeamsServerCredentialsIndexCmd executes the organizations-teams-server-credentials-index command.
func runOrganizationsTeamsServerCredentialsIndexCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, organizationsTeamsServerCredentialsIndexCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, organizationsTeamsServerCredentialsIndexCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.OrganizationsTeamsServerCredentialsIndexRequest](cmd, organizationsTeamsServerCredentialsIndexCmdMeta, "", "")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.ServerCredentials.OrganizationsTeamsServerCredentialsIndex(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
